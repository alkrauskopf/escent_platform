<div >
  <div style="text-align:center;  word-wrap:break-word; color:#2061a5;  font-size: 18px; font-style: normal; font-weight: bold">
			<br/>Maintain Finalized&nbsp;<%= @current_application.abbrev %>&nbsp; Sessions
	</div>
  <%= render :partial => "apps/shared/app_owner_button", :locals=>{:app=>@current_application} %>
  <table  style="width:850px; margin-left:70px; color: #000000; ">
    <tbody>
      <tr>
        <td  style="width:850px; width:fixed; font-size: 12px; font-style: normal; vertical-align:middle; text-align:left;padding-bottom:0px; padding-top:0px; padding-left:0px">
          This allows <%= @current_application.abbrev %> Owners to perform maintenance functions "Finalized" observations.  Operations currently supported are as follows:<br/>
          <strong>Destroy:</strong>  Permanently deletes an observation and related data. Aggregated data tables will be adjusted.  This operation is not reversable.
          <br/>
        </td>
      </tr>
    </tbody>
  </table>
  <% @current_application.organizations.sort_by{|o| o.name}.each do |org| -%>
    <% unless org.tlt_sessions.final.empty? -%>
      <table id="<%=org.public_id%>_school_listing"   style="width:750px; margin-left:50px; color: #2061a5; ">
      <style type="text/css" media="screen">
        table,tr,td { border:none; }
        table tr:hover{
            background-color: transparent;
            }
      </style>
      <tbody>
        <tr>
          <td class="resource_levels_close">
            <%= link_to '', :controller => "/apps/owner_maintenance", :action => :ctl_school_teacher_listing, :organization_id => org, :app_id=> @current_application.id %>
            <%= org.medium_name%>
            <span></span>
          </td>
        </tr>
      </tbody>
    </table>
    <script type="text/javascript" charset="utf-8">
    jQ(document).ready(function() {
      jQ('#<%=org.public_id%>_school_listing .resource_levels_close').toggleResourceLevels();
      jQ('#<%=org.public_id%>_school_listing .resource_levels_open').toggleResourceLevels();
    });
    </script>
    <% end -%>
  <% end -%>
  </br/>
</div>







