Dear <%= @guardian_first_name %>:
<br/><br/>
<%= @student_first_name %> has created a new SAT/ACT personal growth plan milestone.
<br />
<br/>
Subject Area => <strong><%= @subject_area %></strong><br/><br/>
Knowledge Strand => <strong><%= @strand_name %></strong><br/><br/>
Mastery Level => <strong><%= @mastery_level %></strong><br/><br/>
<span style="text-decoration:underline; font-size:16px;"><%= @student_first_name %> Identified The Following Action Plan</span>
<ul>
  <span style="font-style:italic; font-size:16px;"><%=simple_format (@milestone.description)%></span>
</ul>
To date, <%= @student_first_name %> has achieved <%=@plan.milestones.achieved.size %>-of-<%=pluralize(@plan.milestones.size, ' milestone')%> toward SAT/ACT goals.
<br/><br/>
<%= render :partial => 'prep_outreach_guardian', :locals => {:school_name=> @school_name, :student_name => @student_first_name,
                                                             :organization_id => @organization.public_id, :guardian_id=>@guardian_id} %>
